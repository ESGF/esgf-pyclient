[tool:pytest]
addopts =
	--strict
	--tb=native
python_files = test_*.py
markers =
	online: mark test to need internet connection
	slow: mark test to be slow

[flake8]
max-line-length = 120
exclude =
    .git,
    __pycache__,
    docs/conf.py,
    old,
    build,
    dist,
